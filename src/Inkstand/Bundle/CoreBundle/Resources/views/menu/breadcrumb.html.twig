{% if item.children %}
    <ol class="breadcrumb breadcrumb-inline">
        {% for child in item.children %}
            {% if loop.last %}
                <li class="active">{{ child.name|trans }}</li>
            {% else %}
                <li><a href="{{ child.uri }}">{{ child.name|trans }}</a></li>
            {% endif %}
        {% endfor %}
    </ol>
{% endif %}